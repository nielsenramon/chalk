{
  "name": "chalk",
  "scripts": {
    "setup": "bin/setup",
    "local": "bundle exec jekyll serve --drafts",
    "publish": "bin/deploy"
  },
  "dependencies": {
    "jquery": "^3.2.1",
    "throttle-debounce-fn": "^1.0.1",
    "fluidbox": "^2.0.5",
    "retinajs": "^2.1.1",
    "webfontloader": "^1.6.28",
    "svgxuse": "^1.2.4"
  }
}
